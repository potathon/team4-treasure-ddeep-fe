import React from 'react';
import styles from './OffPage.module.css';
import Layout from '../../components/layout/Layout';

const LoadingPC: React.FC = () => {
  return (
    <Layout>
      <div className={styles.container}></div>
    </Layout>
  );
};

export default LoadingPC;
